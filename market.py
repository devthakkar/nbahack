class market:

    def __init__:
        self.available_stocks = available_stocks
        self.users = users
        self.date = date
        self.size = size
        self.total_value = total_value
    
    def transaction(type, user, player, quantity):
        if (type == 'buy'):
            user.stocks[player] = user.stocks[player] + quantity
            user.wallet = user.wallet - quantity
            
        else:



    
        